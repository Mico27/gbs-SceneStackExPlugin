# SceneStackExPlugin — unit test projects

Generated by `generate_test_projects.js` in the GBStudio Plugin Patch Builder. Each folder is
a GB Studio project that installs this plugin in one configuration and exists to prove that
configuration still compiles. Build them all with `build_examples.js --tests`.

Every project carries one scene of each scene type, because GB Studio only compiles the state
file for a scene type the project uses.

| Project | What it covers |
|---|---|
| `DEFAULT` | SceneStackExPlugin on its own, every engine setting at its default. |
| `ALT_Cont_M` | SceneStackExPlugin with ContinuousScenePlugin, MetaTilePlugin — selects engineAlt/Cont_M. |
| `ALT_ScreenS_M` | SceneStackExPlugin with ScreenScrollPlugin, MetaTilePlugin — selects engineAlt/ScreenS_M. |
| `ALT_Cont` | SceneStackExPlugin with ContinuousScenePlugin — selects engineAlt/Cont. |
| `ALT_M` | SceneStackExPlugin with MetaTilePlugin — selects engineAlt/M. |
| `ALT_ScreenS` | SceneStackExPlugin with ScreenScrollPlugin — selects engineAlt/ScreenS. |
